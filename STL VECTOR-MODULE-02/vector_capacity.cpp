#include<bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int>v;
    // cout<<v.capacity()<<endl;

    // cout<<v.size(); type-01
    // cout<<v.max_size(); type-2

    //type-03
    // v.push_back(10);
    // cout<<v.capacity()<<endl;
    // v.push_back(20);
    // cout<<v.capacity()<<endl;
    // v.push_back(30);
    // cout<<v.capacity()<<endl;
    // v.push_back(40);
    // cout<<v.capacity()<<endl;
    // v.push_back(50);
    // cout<<v.capacity()<<endl;

    //type-04
    // vector<int>v={1,2,3,4,5};//not work for this -just works for rng b for loop--here size isnt need
    // vector<int>v(5);//need size-works
    // v.push_back(10);
    // v.push_back(20);
    // v.push_back(30);
    // v.push_back(40);
    // v.push_back(50);
    // v.clear();-its clear size
    // for(int i=0;i<5;i++)
    // {
    //     cout<<v[i]<<" ";
    // }

    //range based for loop
    //  for(int s:v)
    // {
    //     cout<<s<<" ";
    // }

    //type five
    // vector<int>v={10,20,30};
    // cout<<v.empty();--it gives true false ans if true gives 1 else 0

    //type six
    // vector<int>v(5);
    // v.resize(3);
    // cout<<v.size();--it changes the size of vector

    
    return 0;
}
